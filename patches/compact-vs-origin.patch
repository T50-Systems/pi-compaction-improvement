diff --git a/apps/desktop/electron/app-store-composer.ts b/apps/desktop/electron/app-store-composer.ts
index fa20317..5ddf95a 100644
--- a/apps/desktop/electron/app-store-composer.ts
+++ b/apps/desktop/electron/app-store-composer.ts
@@ -608,9 +608,9 @@ async function runComposerCommand(
   }
 
   if (parsed.type === "compact") {
-    await store.driver.compactSession(sessionRef, parsed.customInstructions);
-    await store.reloadTranscriptFromDriver(sessionRef);
-    return finishComposerCommand(store, sessionRef, key, "Compacted session context");
+    const snapshot = finishComposerCommand(store, sessionRef, key, "Compacting session context…");
+    void compactSessionInBackground(store, sessionRef, parsed.customInstructions);
+    return snapshot;
   }
 
   if (parsed.type === "reload") {
@@ -630,14 +630,66 @@ function appendLocalActivity(store: AppStoreInternals, sessionRef: SessionRef, l
   store.sessionState.transcriptCache.set(key, transcript);
 }
 
+function emitLocalActivity(store: AppStoreInternals, sessionRef: SessionRef, label: string): DesktopAppState {
+  const key = sessionKey(sessionRef);
+  appendLocalActivity(store, sessionRef, label);
+  const transcript = store.sessionState.transcriptCache.get(key) ?? [];
+  const preview = previewFromTranscript(transcript);
+  store.state = {
+    ...store.state,
+    workspaces: store.state.workspaces.map((workspace) =>
+      workspace.id === sessionRef.workspaceId
+        ? {
+            ...workspace,
+            sessions: workspace.sessions.map((session) =>
+              session.id === sessionRef.sessionId
+                ? {
+                    ...session,
+                    preview: preview ?? session.preview,
+                    config: store.sessionState.sessionConfigBySession.get(key),
+                  }
+                : session,
+            ),
+          }
+        : workspace,
+    ),
+    lastError: undefined,
+    revision: store.state.revision + 1,
+  };
+  store.schedulePersistUiState();
+  const snapshot = store.emit();
+  store.publishSelectedTranscriptFor(sessionRef);
+  return snapshot;
+}
+
+async function compactSessionInBackground(
+  store: AppStoreInternals,
+  sessionRef: SessionRef,
+  customInstructions?: string,
+): Promise<void> {
+  try {
+    await store.driver.compactSession(sessionRef, customInstructions);
+    await store.reloadTranscriptFromDriver(sessionRef);
+    emitLocalActivity(store, sessionRef, "Compacted session context");
+  } catch (error) {
+    const message = error instanceof Error ? error.message : String(error);
+    emitLocalActivity(store, sessionRef, `Compaction failed · ${message}`);
+    await store.withSessionError(sessionRef, error);
+  }
+}
+
 function finishComposerCommand(
   store: AppStoreInternals,
   sessionRef: SessionRef,
   key: string,
   label: string,
-): DesktopAppState {
-  store.sessionState.composerDraftsBySession.delete(key);
-  store.sessionState.composerAttachmentsBySession.delete(key);
+  clearComposer = true,
+  clearLastError = true,
+ ): DesktopAppState {
+  if (clearComposer) {
+    store.sessionState.composerDraftsBySession.delete(key);
+    store.sessionState.composerAttachmentsBySession.delete(key);
+  }
   appendLocalActivity(store, sessionRef, label);
   const transcript = store.sessionState.transcriptCache.get(key) ?? [];
   const preview = previewFromTranscript(transcript);
@@ -659,11 +711,15 @@ function finishComposerCommand(
           }
         : workspace,
     ),
-    composerDraft: "",
-    composerDraftSyncSource: "command",
-    composerDraftSyncNonce: store.allocateComposerDraftSyncNonce(),
-    composerAttachments: [],
-    lastError: undefined,
+    ...(clearComposer
+      ? {
+          composerDraft: "",
+          composerDraftSyncSource: "command" as const,
+          composerDraftSyncNonce: store.allocateComposerDraftSyncNonce(),
+          composerAttachments: [],
+        }
+      : {}),
+    ...(clearLastError ? { lastError: undefined } : {}),
     revision: store.state.revision + 1,
   };
   store.schedulePersistUiState();
diff --git a/apps/desktop/src/timeline-item.tsx b/apps/desktop/src/timeline-item.tsx
index 4167528..6701566 100644
--- a/apps/desktop/src/timeline-item.tsx
+++ b/apps/desktop/src/timeline-item.tsx
@@ -1,3 +1,5 @@
+import { memo } from "react";
+import { Button } from "@/components/ui/button";
 import type { SessionTranscriptMessage } from "@pi-gui/pi-sdk-driver";
 import type { TimelineActivity, TimelineToolCall, TimelineSummary, TranscriptMessage } from "./timeline-types";
 import { MessageMarkdown } from "./message-markdown";
@@ -5,17 +7,19 @@ import { InlineDiff, extractDiffFromOutput } from "./diff-inline";
 import { ChevronRightIcon, CopyIcon, DiffIcon, FileIcon } from "./icons";
 import { extensionToLanguage } from "./syntax-highlight";
 
-export function TimelineItem({
-  item,
-  expandedToolCallIds,
-  onToggleToolCall,
-  onViewFileInDiff,
-}: {
+interface TimelineItemProps {
   readonly item: TranscriptMessage;
-  readonly expandedToolCallIds?: ReadonlySet<string>;
+  readonly toolExpanded?: boolean;
   readonly onToggleToolCall?: (callId: string) => void;
   readonly onViewFileInDiff?: (path: string) => void;
-}) {
+}
+
+export const TimelineItem = memo(function TimelineItem({
+  item,
+  toolExpanded = false,
+  onToggleToolCall,
+  onViewFileInDiff,
+}: TimelineItemProps) {
   switch (item.kind) {
     case "message":
       return <TimelineMessage item={item} />;
@@ -25,7 +29,7 @@ export function TimelineItem({
       return (
         <TimelineToolCallItem
           item={item}
-          expanded={expandedToolCallIds?.has(item.callId) ?? false}
+          expanded={toolExpanded}
           onToggle={onToggleToolCall}
           onViewFileInDiff={onViewFileInDiff}
         />
@@ -35,7 +39,7 @@ export function TimelineItem({
     default:
       return null;
   }
-}
+});
 
 function TimelineMessage({ item }: { readonly item: SessionTranscriptMessage }) {
   if (item.role === "user") {
@@ -119,6 +123,7 @@ function TimelineToolCallItem({
   const filePath = isWriteTool(item.toolName) ? extractFilename(item.input) || undefined : undefined;
   const diffLanguage = diffText && filePath ? extensionToLanguage(filePath) : undefined;
   const inlineDetail = item.status === "error" ? item.detail : undefined;
+  const compactMeta = buildCompactMeta(item);
 
   const handleCopy = () => {
     const text = diffText ?? formatToolContent(item.input, item.output);
@@ -128,11 +133,12 @@ function TimelineToolCallItem({
   return (
     <article className={`timeline-tool timeline-tool--${item.status}`}>
       <div className="timeline-tool__header-row">
-        <button
+        <Button
           className="timeline-tool__header"
           type="button"
           aria-expanded={expanded}
           disabled={!hasContent}
+          variant="ghost"
           onClick={() => onToggle?.(item.callId)}
         >
           {hasContent ? (
@@ -144,26 +150,27 @@ function TimelineToolCallItem({
           {inlineDetail ? <span className="timeline-tool__detail">{inlineDetail}</span> : null}
           {diffStats ? (
             <span className="timeline-tool__diff-stats">
-              <span className="timeline-tool__stat-add">+{diffStats.added}</span>
-              {" "}
+              <span className="timeline-tool__stat-add">+{diffStats.added}</span>{" "}
               <span className="timeline-tool__stat-del">-{diffStats.removed}</span>
             </span>
           ) : null}
-          <span className="timeline-tool__meta-inline">{`${item.toolName} \u00b7 ${statusLabel(item.status)}`}</span>
-        </button>
+          <span className="timeline-tool__meta-inline">{compactMeta}</span>
+        </Button>
         {filePath && onViewFileInDiff ? (
-          <button
+          <Button
             aria-label={`View ${filePath} in changes`}
             className="icon-button timeline-tool__view-in-diff"
             data-testid="timeline-tool-view-in-diff"
+            size="icon"
             type="button"
+            variant="ghost"
             onClick={(event) => {
               event.stopPropagation();
               onViewFileInDiff(filePath);
             }}
           >
             <DiffIcon />
-          </button>
+          </Button>
         ) : null}
       </div>
       {expanded && hasContent ? (
@@ -180,18 +187,18 @@ function TimelineToolCallItem({
                     </span>
                   ) : null}
                 </span>
-                <button className="icon-button timeline-tool__copy" type="button" onClick={handleCopy} aria-label="Copy">
+                <Button className="icon-button timeline-tool__copy" size="icon" type="button" variant="ghost" onClick={handleCopy} aria-label="Copy">
                   <CopyIcon />
-                </button>
+                </Button>
               </div>
               <InlineDiff diff={diffText} language={diffLanguage} />
             </>
           ) : (
             <>
               <div className="timeline-tool__body-actions">
-                <button className="icon-button timeline-tool__copy" type="button" onClick={handleCopy} aria-label="Copy">
+                <Button className="icon-button timeline-tool__copy" size="icon" type="button" variant="ghost" onClick={handleCopy} aria-label="Copy">
                   <CopyIcon />
-                </button>
+                </Button>
               </div>
               <pre className="timeline-tool__pre">{formatToolContent(item.input, item.output)}</pre>
             </>
@@ -213,9 +220,30 @@ function buildCompactLabel(item: TimelineToolCall, diffStats: { added: number; r
       return `Edited ${shortenPath(filename)}`;
     }
   }
+
+  if (/read/i.test(item.toolName)) {
+    const filename = extractFilename(item.input);
+    if (filename) {
+      return `Read ${shortenPath(filename)}`;
+    }
+  }
+
+  if (/bash|command|shell/i.test(item.toolName)) {
+    return diffStats ? `Ran command · ${diffStats.added}+ ${diffStats.removed}-` : "Ran command";
+  }
+
   return item.label;
 }
 
+function buildCompactMeta(item: TimelineToolCall): string {
+  if (item.status === "running") return "running";
+  if (item.status === "error") return "failed";
+  if (/read/i.test(item.toolName)) return "read";
+  if (isWriteTool(item.toolName)) return "edit";
+  if (/bash|command|shell/i.test(item.toolName)) return "command";
+  return item.toolName;
+}
+
 function extractFilename(input: unknown): string {
   if (typeof input === "object" && input !== null) {
     const record = input as Record<string, unknown>;
@@ -228,10 +256,10 @@ function extractFilename(input: unknown): string {
 }
 
 function shortenPath(filePath: string): string {
-  // Show last 2-3 path segments for readability
-  const parts = filePath.split("/");
+  const normalized = filePath.replaceAll("\\", "/");
+  const parts = normalized.split("/");
   if (parts.length <= 3) {
-    return filePath;
+    return normalized;
   }
   return parts.slice(-3).join("/");
 }
diff --git a/apps/desktop/src/tree-modal.tsx b/apps/desktop/src/tree-modal.tsx
index d10c923..76c2c61 100644
--- a/apps/desktop/src/tree-modal.tsx
+++ b/apps/desktop/src/tree-modal.tsx
@@ -1,4 +1,8 @@
 import { useEffect, useLayoutEffect, useMemo, useRef, useState, type KeyboardEvent as ReactKeyboardEvent } from "react";
+import { Button } from "@/components/ui/button";
+import { Dialog, DialogContent } from "@/components/ui/dialog";
+import { Input } from "@/components/ui/input";
+import { Textarea } from "@/components/ui/textarea";
 import type {
   NavigateSessionTreeOptions,
   SessionTreeNodeKind,
@@ -240,21 +244,15 @@ export function TreeModal({
   };
 
   return (
-    <div
-      className="tree-modal-backdrop"
-      onMouseDown={(event) => {
-        if (event.target !== event.currentTarget || step !== "select" || submitting) {
-          return;
-        }
+    <Dialog open onOpenChange={(open) => {
+      if (!open) {
         onClose();
-      }}
-    >
-      <div
-        aria-modal="true"
+      }
+    }}>
+      <DialogContent
         className="tree-modal"
         data-testid="tree-modal"
         ref={dialogRef}
-        role="dialog"
         tabIndex={-1}
         onKeyDown={handleKeyDown}
       >
@@ -263,11 +261,13 @@ export function TreeModal({
             <div className="tree-modal__eyebrow">Session tree</div>
             <h2 className="tree-modal__title">{step === "summary" ? "Switch branch" : "Browse branches"}</h2>
           </div>
-          <button
+          <Button
             aria-label="Close tree modal"
             className="tree-modal__close"
             disabled={submitting}
+            size="icon"
             type="button"
+            variant="ghost"
             onClick={() => {
               if (step === "summary") {
                 setStep("select");
@@ -277,7 +277,7 @@ export function TreeModal({
             }}
           >
             ×
-          </button>
+          </Button>
         </div>
 
         {error ? (
@@ -295,13 +295,13 @@ export function TreeModal({
         {!loading && tree && step === "select" ? (
           <>
             <div className="tree-modal__search-row">
-              <input
+              <Input
                 autoFocus
                 aria-label="Search session tree"
                 className="tree-modal__search"
                 data-testid="tree-modal-search"
-                placeholder="Search visible tree entries"
                 ref={searchRef}
+                placeholder="Search visible tree entries"
                 value={search}
                 onChange={(event) => {
                   cancelAutoScroll();
@@ -316,7 +316,6 @@ export function TreeModal({
                     : "Select a node to branch from it."}
               </div>
             </div>
-
             <div className="tree-modal__list" data-testid="tree-modal-list" ref={setListElement}>
               {displayRows.length === 0 ? (
                 <div className="tree-modal__empty">No matching nodes.</div>
@@ -329,22 +328,24 @@ export function TreeModal({
                       className={`tree-row ${isSelected ? "tree-row--selected" : ""} ${isCurrentLeaf ? "tree-row--active" : ""}`}
                       key={row.node.id}
                     >
-                      <button
+                      <Button
                         aria-label={row.expanded ? "Collapse branch" : "Expand branch"}
-                        className={`tree-row__toggle ${row.hasChildren ? "" : "tree-row__toggle--hidden"}`}
+                        className={`tree-row__toggle ${row.hasChildren ? "" : "tree-row__toggle--hidden"}`.trim()}
                         disabled={searching || !row.hasChildren}
                         tabIndex={-1}
                         type="button"
+                        variant="ghost"
                         onClick={() => handleToggleExpanded(row.node.id)}
                       >
                         {row.hasChildren ? row.expanded ? <ChevronDownIcon /> : <ChevronRightIcon /> : null}
-                      </button>
-                      <button
+                      </Button>
+                      <Button
                         className="tree-row__content"
                         data-tree-selected={isSelected ? "true" : undefined}
                         data-testid={`tree-row-${row.node.id}`}
                         title={buildTreeRowLine(row, currentLeafId)}
                         type="button"
+                        variant="ghost"
                         onClick={() => {
                           cancelAutoScroll();
                           setSelectedId(row.node.id);
@@ -358,7 +359,7 @@ export function TreeModal({
                         }}
                       >
                         <span className="tree-row__line">{buildTreeRowLine(row, currentLeafId)}</span>
-                      </button>
+                      </Button>
                     </div>
                   );
                 })
@@ -370,17 +371,17 @@ export function TreeModal({
                 Selecting a user prompt reopens it in the composer. Selecting any other node jumps directly there.
               </div>
               <div className="tree-modal__actions">
-                <button className="button button--secondary" type="button" onClick={onClose}>
+                <Button className="button button--secondary" type="button" variant="secondary" onClick={onClose}>
                   Cancel
-                </button>
-                <button
+                </Button>
+                <Button
                   className="button button--primary"
                   disabled={!selectedId || currentLeafSelected}
                   type="button"
                   onClick={() => setStep("summary")}
                 >
                   {currentLeafSelected ? "Already here" : "Continue"}
-                </button>
+                </Button>
               </div>
             </div>
           </>
@@ -392,34 +393,37 @@ export function TreeModal({
               You&apos;re leaving the current branch. Choose whether pi should summarize the abandoned path before switching.
             </div>
             <div className="tree-summary-options">
-              <button
-                className={`tree-summary-option ${summaryMode === "none" ? "tree-summary-option--selected" : ""}`}
+              <Button
+                className={`tree-summary-option ${summaryMode === "none" ? "tree-summary-option--selected" : ""}`.trim()}
                 type="button"
+                variant={summaryMode === "none" ? "default" : "ghost"}
                 onClick={() => setSummaryMode("none")}
               >
                 <span className="tree-summary-option__title">No summary</span>
                 <span className="tree-summary-option__description">Jump immediately with no branch summary.</span>
-              </button>
-              <button
-                className={`tree-summary-option ${summaryMode === "summary" ? "tree-summary-option--selected" : ""}`}
+              </Button>
+              <Button
+                className={`tree-summary-option ${summaryMode === "summary" ? "tree-summary-option--selected" : ""}`.trim()}
                 type="button"
+                variant={summaryMode === "summary" ? "default" : "ghost"}
                 onClick={() => setSummaryMode("summary")}
               >
                 <span className="tree-summary-option__title">Summarize</span>
                 <span className="tree-summary-option__description">Generate a branch summary before switching.</span>
-              </button>
-              <button
-                className={`tree-summary-option ${summaryMode === "custom" ? "tree-summary-option--selected" : ""}`}
+              </Button>
+              <Button
+                className={`tree-summary-option ${summaryMode === "custom" ? "tree-summary-option--selected" : ""}`.trim()}
                 type="button"
+                variant={summaryMode === "custom" ? "default" : "ghost"}
                 onClick={() => setSummaryMode("custom")}
               >
                 <span className="tree-summary-option__title">Summarize with custom prompt</span>
                 <span className="tree-summary-option__description">Provide extra instructions for the summary.</span>
-              </button>
+              </Button>
             </div>
 
             {summaryMode === "custom" ? (
-              <textarea
+              <Textarea
                 autoFocus
                 aria-label="Custom summary instructions"
                 className="tree-modal__custom-instructions"
@@ -439,15 +443,16 @@ export function TreeModal({
                     : "The summary will be attached to the branch you switch to."}
               </div>
               <div className="tree-modal__actions">
-                <button
+                <Button
                   className="button button--secondary"
                   disabled={submitting}
                   type="button"
+                  variant="secondary"
                   onClick={() => setStep("select")}
                 >
                   Back
-                </button>
-                <button
+                </Button>
+                <Button
                   className="button button--primary"
                   data-tree-summary-confirm="true"
                   disabled={submitting || !selectedId || (summaryMode === "custom" && customInstructions.trim().length === 0)}
@@ -455,13 +460,13 @@ export function TreeModal({
                   onClick={handleSubmit}
                 >
                   {submitting ? "Switching…" : "Switch branch"}
-                </button>
+                </Button>
               </div>
             </div>
           </div>
         ) : null}
-      </div>
-    </div>
+      </DialogContent>
+    </Dialog>
   );
 }
 
